version: 1
indices:
  categories:
    include:
      - /category/**
    properties:
      title:
        select: head > title
      description:
        select: head > meta[name="description"]
      image:
        select: head > meta[property="og:image"]
      path:
        select: head > link[rel="canonical"]
    target: /category